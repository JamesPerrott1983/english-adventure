/* English Adventure — approved lessons baked into the app.
   This file is empty until a teacher uses "Publish to the app" in the
   Teacher Area, downloads the generated custom-lessons.js, and replaces
   this file with it. The published lessons then override the built-in
   curriculum on every computer that runs this copy of the app. */
window.EA = window.EA || {};
EA.builtInLessons = EA.builtInLessons || [];
